🏦 End-to-End Credit Scoring Pipeline & Profit Optimization
โปรเจกต์นี้คือ Data Pipeline สำหรับวิเคราะห์และประเมินความเสี่ยงสินเชื่อ (Credit Scoring) แบบครบวงจร ตั้งแต่การดึงข้อมูลดิบจาก Kaggle, การทำ Feature Engineering ด้วย SQL & Python, การสร้างโมเดล Machine Learning (XGBoost) ไปจนถึงการหาสมดุลทางธุรกิจ (Profit Maximization) และส่งผลลัพธ์ขึ้น Cloud เพื่อแสดงผลบน Dashboard

🔗 Live Dashboard: https://lookerstudio.google.com/reporting/deea8fe1-f48c-417a-baf7-470f2f878c8f

📌 Overview (ภาพรวมโปรเจกต์)
ระบบนี้ถูกออกแบบมาเพื่อแก้ปัญหา Imbalanced Data ในข้อมูลสินเชื่อ และไม่ได้ดูแค่ความแม่นยำของโมเดลเพียงอย่างเดียว แต่ยังคำนวณ "จุดตัดที่คุ้มทุนที่สุด" (Optimal Threshold) เพื่อสร้างกำไรสูงสุดให้กับพอร์ตโฟลิโอ

🚀 Key Features

Automated Ingestion: ดึงข้อมูลอัตโนมัติจาก Kaggle API

Hybrid Processing: ใช้ SQLite สำหรับ SQL Transformation และ Pandas สำหรับ Data Manipulation

Advanced Modeling:

แก้ปัญหาข้อมูลไม่สมดุลด้วย SMOTE

ใช้ XGBoost Classifier ทำนายโอกาสการผิดนัดชำระหนี้ (Probability of Default)

Calibrated Classifier เพื่อปรับค่าความน่าจะเป็นให้แม่นยำขึ้น

Business Logic Optimization: อัลกอริทึมค้นหา Threshold ที่สร้างกำไรสูงสุด (Max Profit) โดยชั่งน้ำหนักระหว่างดอกเบี้ยที่ได้รับ vs เงินต้นที่สูญเสีย

Cloud Integration: เชื่อมต่อและอัปโหลดข้อมูลไปยัง Google BigQuery

Visualization: แสดงผลผ่าน Looker Studio

🛠 Architecture Pipeline
ข้อมูลโค้ด
graph LR
    A[Kaggle Dataset] -->|Download| B(Python / Google Colab)
    B -->|SQL Transform| C[SQLite In-Memory]
    C -->|Feature Eng.| D[Pandas DataFrame]
    D -->|SMOTE & XGBoost| E[Risk Model]
    E -->|Profit Matrix Loop| F[Optimization Engine]
    F -->|Upload Results| G[Google BigQuery]
    G -->|Connect| H[Looker Studio Dashboard]
⚙️ Tech Stack
Language: Python

Libraries: pandas, numpy, sqlite3, xgboost, sklearn, imblearn, seaborn

Cloud Service: Google BigQuery

Visualization: Looker Studio

Data Source: Lending Club Loan Data (Kaggle)

📝 Process Detail (ขั้นตอนการทำงาน)
1. Data Loading & Transformation

ดาวน์โหลดข้อมูลสินเชื่อ (Lending Club) ผ่าน kagglehub

สร้าง Database จำลอง (SQLite in-memory) เพื่อใช้ SQL จัดกลุ่ม FICO Score และแปลงข้อมูลทางการเงิน

2. Modeling (XGBoost + SMOTE)

Problem: ข้อมูลสินเชื่อส่วนใหญ่มักเป็นหนี้ดี (Class 0) ทำให้โมเดลลำเอียง

Solution: ใช้ SMOTE (Synthetic Minority Over-sampling Technique) สังเคราะห์ข้อมูลหนี้เสียเพิ่มขึ้นในขั้นตอน Train เพื่อให้โมเดลเรียนรู้ได้สมดุล

Algorithm: ใช้ XGBoost เนื่องจากมีประสิทธิภาพสูงกับข้อมูลตาราง และปรับ scale_pos_weight

3. Profit Maximization (Business Value) 💰

แทนที่จะใช้ Threshold มาตรฐานที่ 0.5 ระบบจะจำลองสถานการณ์ทางธุรกิจ:

Profit per Good Loan: +1,000 THB (สมมติ)

Cost of Default: -5,000 THB (สมมติ)

ระบบจะวนลูปความเสี่ยงตั้งแต่ 0-100% เพื่อหาจุดที่ กำไรโดยรวม (Total Profit) สูงที่สุด แล้วนำจุดนั้นมาเป็นเกณฑ์อนุมัติ (Cut-off)

4. Deployment

อัปโหลดตารางผลลัพธ์ 2 ตารางขึ้น BigQuery:

loan_risk_report: รายละเอียดลูกค้าแต่ละราย พร้อมสถานะ Approve/Reject และ Risk Grade

profit_optimization: ตารางจำลองผลกำไรในแต่ละ Threshold เพื่อใช้ทำกราฟวิเคราะห์

💻 How to Run
Prerequisites:

Google Account (สำหรับ Colab และ BigQuery)

Google Cloud Project ที่เปิดใช้งาน BigQuery API แล้ว

Configuration:

แก้ไขตัวแปร project_id ในโค้ดให้เป็น Project ID ของคุณ

Execution:

รันสคริปต์บน Google Colab (แนะนำ) เนื่องจากมีการใช้ auth.authenticate_user() เพื่อยืนยันตัวตนเข้า GCP ง่ายๆ

Python
# ตัวอย่างคำสั่ง Install Library
!pip install kagglehub pandas-gbq db-sqlite3 xgboost imbalanced-learn
📊 Dashboard Preview

<img width="883" height="651" alt="image" src="https://github.com/user-attachments/assets/58e49dec-4b25-4720-8644-76425d47ab05" />

<img width="895" height="622" alt="image" src="https://github.com/user-attachments/assets/67520668-41b7-46c0-a8af-0af51f3da31e" />

<img width="873" height="613" alt="image" src="https://github.com/user-attachments/assets/4eaead49-ae1a-4509-9a4d-cc75afc7200e" />

<img width="671" height="569" alt="image" src="https://github.com/user-attachments/assets/e97922ab-d52c-4e8b-a0df-3b9a2dd41154" />




Metrics ที่แสดงผล:

Approval Rate: อัตราการอนุมัติสินเชื่อ

Expected NPL: หนี้เสียที่คาดการณ์ในพอร์ตใหม่

Risk Grade Distribution: การกระจายตัวของเกรดลูกค้า (A-F)

Projected Profit: กำไรที่คาดว่าจะได้รับ
